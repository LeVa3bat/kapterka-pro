<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Preserve application data by default, but never restore the physical
         device identity to another phone. A restored phone must register
         itself as a new sync device. -->
    <exclude domain="sharedpref" path="kapterka_sync_prefs.xml" />
</full-backup-content>
