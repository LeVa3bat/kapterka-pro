# Каптёрка ПРО — контекст для Claude Code

**Проект:** Android-приложение для учёта склада и имущества подразделения (военные, МВД, спасатели).
**Текущая версия:** 3.6.0 (код 33, выпущена 24.09.2026)
**Статус:** готово к продаже, опубликовано на сайте, RuStore, Telegram, 4PDA.

---

## Быстрый старт для новой сессии

1. Основной план: **[ROADMAP.md](ROADMAP.md)** — что делаем сейчас и в следующих версиях.
2. Что реализовано в 3.6.0: **[RELEASE_NOTES_v3.6.0.md](RELEASE_NOTES_v3.6.0.md)**.
3. Текущее состояние приложения и сервера — см. ниже.

---

## Техстек

- **App:** Kotlin/Jetpack Compose, Room (локальная БД), Firebase Firestore (синхронизация), ZXing (QR).
- **Backend:** Cloudflare Worker (kapterka-api), Firestore REST, Gmail Apps Script (почта).
- **Платёж:** ЮKassa (HTTPS → сервер → приложение, ключ на сервере, не в коде).
- **Сборка:** Gradle, R8 (код-обрезка до ~5 МБ), CI/CD на GitHub Actions.
- **Тесты:** JUnit, Roborazzi (снимки экранов), Firestore эмулятор, сервер — Node.js.
- **Сайт:** GitHub Pages из `docs/`, HTML/CSS/JS, IndexNow, JSON-LD.

---

## Основные файлы и функции

### Приложение (`app/`)

| Экран / Функция | Файл | Статус |
|---|---|---|
| Главная (доска остатков) | `MainDashboardScreen.kt` | ✅ Компактная, долгое нажатие → переместить/удалить |
| Операции (приход/выдача) | `OperationsScreen.kt`, `OperationDialogs.kt` | ✅ Окна с проверкой остатка |
| Журнал по дням | `HistoryScreen.kt` | ✅ Группировка, фильтр периода (7/30 дней) |
| Заявки | `RequestsScreen.kt`, `RequisitionRequest.kt` | ✅ Доска по этапам, статусы PENDING/ASSEMBLING/COLLECTED/ISSUED |
| Подключение по QR | `UnitKeySyncDialog.kt`, `UnitQr.kt` | ✅ QR-код, сканирование, копирование |
| Лицензия | `PersonalLicenseDialog.kt` | ✅ Новое окно, срок, оплата, восстановление |
| Отчёты | `ExcelReportPreviewDialog.kt` | ✅ Форма 8 (раздаточная), форма 18 (книга учёта) |
| Справка | `UserManualDialog.kt`, `CommunityLinks.kt` | ✅ 8 разделов, ссылки на сайт/Telegram |
| Настройки | `MoreSettingsScreen.kt`, `DeveloperBackdoorDialog.kt` | ✅ Профиль, синхронизация, статистика |
| UI компоненты | `ModernBottomBar.kt`, `AnimatedCheck.kt`, `SplashScreen.kt` | ✅ Нижняя панель, анимации, вибрация |

### Сервер (`server/cloudflare/`)

| Функция | Файл | Статус |
|---|---|---|
| Почта (с кодом подтверждения) | `worker.mjs` → `mail_diag` | ✅ Диагностика, таймаут 28 с |
| Оплата (ЮKassa) | `worker.mjs` → `create`, `check` | ✅ Безопасно, обработка ошибок |
| Удаление тестовых пользователей | `worker.mjs` → `admin_delete_fighter` | ✅ Защита лицензий |
| Тесты | `worker.test.mjs` | ✅ 35/35 зелёные |

### Сайт (`docs/`)

| Раздел | Файл | Статус |
|---|---|---|
| Главная | `index.html` | ✅ Герой, компактная доверие, галерея, FAQ, CTA |
| Руководство | `rukovodstvo-polzovatelya.html` | ✅ 8 разделов с примерами |
| Форма 8 | `forma-8-razdatochnaya-vedomost.html` | ✅ SEO-страница |
| Программа для старшины | `programma-dlya-starshiny-roty.html` | ✅ SEO-страница |
| Стили | `site36.css` | ✅ Адаптивный дизайн |
| Скрипты | `site36.js`, `app.js` | ✅ Slideshow, Metrika, галерея |

---

## Текущее состояние (3.6.0)

### ✅ Реализовано

- Новый интерфейс: плавающая нижняя панель, анимации, вибро-отклик.
- Подключение по QR-коду, QR видно на экране.
- Заявки по этапам с выдачей со склада.
- Журнал по дням, фильтр 7/30 дней.
- Формы 8 и 18 в Excel.
- Новое окно лицензии.
- Встроенное руководство.
- Синхронизация работает (Firestore).
- Почта: код приходит.
- Оплата: ключи ЮKassa действительны.
- Модульные тесты: сервер 35/35, приложение — снимки экранов.
- Сайт: современный дизайн, SEO.
- Размер APK: ~5 МБ.

### ⚠️ Известные ограничения

- Синхронизация работает, но у части операторов только через VPN (Firebase GCP блокируется).
- Строгие правила Firestore включены, но в переходном режиме (чтение без проверки).
- Нет сбора отчётов о сбоях.
- Нет резервной копии базы в файл.
- Нет счётчиков на фильтрах журнала.

---

## Что планируем дальше

### 3.7.0 — «Надёжность»

1. **Синхронизация без VPN** — обмен через `kapterka-api` (Cloudflare) вместо Firebase.
2. **Резервная копия** — сохранить в файл, восстановить, автокопия раз в неделю.
3. **Отчёты о сбоях** (AppMetrica или Crashlytics).
4. **Статус синхронизации** — значок на главной, подробности по нажатию.
5. **Строгие правила Firestore** — полная проверка доступа.

### 3.8.0 — «Оплата»

- Напоминание о конце пробного периода.
- Тарифы 3–6 месяцев со скидкой.
- Просьба об оценке в RuStore.
- «Пригласи друга» → +7 дней обоим.
- Счётчики на фильтрах.

### 3.9.0 — «Склад»

- Сканер штрих-кодов имущества.
- Карточка человека.
- Инвентаризация.
- Импорт из Excel.
- PDF форм.

Подробнее: [ROADMAP.md](ROADMAP.md).

---

## Команды для запуска

```bash
# Собрать и запустить приложение
./gradlew assembleDebug

# Запустить тесты
./gradlew test

# Снимки экранов (Roborazzi)
./gradlew recordRoborazziDebug

# Запустить сервер (локально)
npm run test --prefix server/cloudflare

# Проверить сайт
npm run test:smoke --prefix .
```

## CI/CD

- **ui-screenshots.yml** — снимки экранов, публикует на ветку `ui-screenshots`.
- **android-tests.yml** — unit-тесты.
- **release-3.6.0.yml** — выпуск (вручную, публикует на main).
- **api-health.yml** — ежедневная проверка сервера, почты, оплаты.
- **firestore-rules.yml** — тесты правил доступа.

## Ветки

- **main** — стабильное (последний релиз: 3.6.0).
- **claude/secure-3.6** — текущая работа (готовится к слиянию).

---

## Публикация

1. **Сайт:** GitHub Pages (кнопка в GitHub → Pages), `docs/` на main.
2. **RuStore:** вручную через консоль (APK, скриншоты 1080×1920, баннер 1920×1080).
3. **Telegram:** @kapterka_pro (посты с баннером и APK).
4. **4PDA:** вручную через релизер (форма + скриншоты + APK).

### Последний релиз: 3.6.0 (24.09.2026)

- Опубликовано: сайт, RuStore, Telegram, 4PDA (релизер).
- Результаты: [RELEASE_NOTES_v3.6.0.md](RELEASE_NOTES_v3.6.0.md).
- 4PDA: отправлена жалоба куратору на обновление шапки.

---

## Контакты и ссылки

- **Сайт:** https://kapterka-pro.ru/
- **RuStore:** https://apps.rustore.ru/app/com.aistudio.kapterka.jmwqve
- **Telegram:** https://t.me/kapterka_pro
- **Разработчик:** Васев Алексей Евгеньевич
- **Пакет:** com.aistudio.kapterka.jmwqve

---

## Правила работы в этом проекте

- **Язык:** русский в коде (комментарии, строки ошибок, UI).
- **Публикация:** только по команде владельца («выкладывай», «публикуй»).
- **Секреты:** никогда не коммитить секреты, пароли, токены; они хранятся только на сервере.
- **Репо:** публичный, никаких личных данных в логах CI.
- **Лицензия:** коммиты подписывать: `Co-Authored-By: Claude <...>`.

---

## Для новой сессии

**Команда:** Читайте [ROADMAP.md](ROADMAP.md) → выберите пункт → пишите план → проверяйте → выкладываю.

**Пример:** «Начинаем 3.7, пункт 1: синхронизация без VPN. Что нужно изменить на сервере и в приложении?»

**Лимиты:** новую задачу начинайте в новой сессии, чтобы не тратить кредиты на перечитывание истории.
